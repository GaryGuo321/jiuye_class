一，未完成部分／待改善部分

1，视频播放次数的统计没有写出来。（能力问题）。。。
2，预约人数，我写的是每点击一次就加一次，显得有点鸡肋。待改善。文件中使用了一个小小的php不需要刻意删掉
3，整一个sass的规划还是有点乱，没有真正的高效率起来。
4，放在服务器上跑的时候，视频播放前预先现实的图片在chrome等webkit内核的浏览器上会报错，提示找不到该照片其他浏览器可以实现。在本地打开的话可以找到图片
5，使用fis进行资源的压缩等等，可以对比起来好像图片并没有被压缩小了啊。。难道是打开浏览器时才压缩？最后是找了压图工具，进行的压缩。
6，fis有设置整理多余资源，样式引用图片好像也没有生成雪碧图，不知道又是为啥？？


二，文档结构解析

1，jQuery动画使用了velocity.js插件
2，banner使用了swiper.js插件
3，在统计次数方面使用了ajax和数据库，使用了php写。（php随便写的。。。只是够用）
4，文件里面有详细的解释。

